version: '3'
services:
  sys:
    image: ubuntu:rolling
    stdin_open: true
    tty: true
    command: /bin/bash
    links:
      - database
  database:
    image: library/mariadb
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD='true'
